const Sequelize = require('sequelize')
const request = require('request');

// let db = require('database');


const getSpotifyAccessToken = new Promise((resolve, reject) => {
	resolve('dit is een test');
});



module.exports = getSpotifyAccessToken;